// TrackGuard — AI Analyser
// Sends raw crawl data to the Anthropic API for expert interpretation.
// Constructs the analysis prompt and parses the structured response.
// See the build prompt for full specification.

export {};
