// TrackGuard — Crawl Engine
// Playwright-based headless browser crawl that captures
// dataLayer events, GA4 requests, consent state, and screenshots.
// See the build prompt for full specification.

export {};
